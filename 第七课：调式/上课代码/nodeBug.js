//语法错误
//va a = 1;//SyntaxError 语法错误

//ReferenceError 引用错误
//在某一位置使用未定义的变量
//console.log(a);

// var username = "zhangsan";
// username();
//TypeError 类型的错误
//特定方法的参数必须是特定类型的变量
// const fs = require("fs");
// fs.readFileSync(true);

/**
 * 1、debugger设置断点
 * 2、node inspect(debug) 文件名
 * 3、c 继续执行 s o
 * 4、watch("变量名");
 * 5、watchers查看监听的变量
 * 6、unwatch("变量名");
 */
