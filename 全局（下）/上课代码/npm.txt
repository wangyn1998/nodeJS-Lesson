1、nrm用来切换第三方模块下载的服务器的
（一个网速较慢，原来的服务器上不存在该模块）

2、install -now 在当前目录安装，模块会被添加到 node_modules
   install date-now -g 全局安装

3、nvmw 进行node版本管理的工具
nvmw install node版本号
nvme uninstall node版本号
nvmw ls 显示所安装的node的版本
nbmw use node版本号 切换node的版本